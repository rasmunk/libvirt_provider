#cloud-config
system_info:
  default_user:
    name: libvirt
    home: /home/libvirt
    sudo: ALL=(ALL) NOPASSWD:ALL
    passwd: "Passw0rd!"
    lock_passwd: false
    shell: /bin/bash
hostname: host-vm-for-remote-test
ssh_pwauth: True
